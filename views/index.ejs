<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>RSS Reader</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body class="bg-gray-50 dark:bg-gray-900 h-screen overflow-hidden">
  <!-- Mobile menu button -->
  <button onclick="toggleSidebar()" class="md:hidden fixed top-4 left-4 z-50 p-2 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-700">
    <svg class="w-6 h-6 text-gray-700 dark:text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
    </svg>
  </button>
  
  <div class="flex h-full">
    <!-- Sidebar -->
    <aside id="sidebar" class="w-64 bg-white dark:bg-gray-800 border-r border-gray-200 dark:border-gray-700 flex flex-col fixed md:relative inset-y-0 left-0 transform -translate-x-full md:translate-x-0 transition-transform duration-300 ease-in-out z-40">
      <!-- Header -->
      <div class="p-4 border-b border-gray-200 dark:border-gray-700">
        <div class="flex items-center justify-between mb-2">
          <h1 class="text-xl font-bold text-gray-800 dark:text-white">RSS Reader</h1>
          <!-- Dark mode toggle -->
          <button onclick="toggleDarkMode()" class="p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-700 transition" title="Toggle dark mode">
            <svg id="theme-toggle-dark-icon" class="hidden w-5 h-5 text-gray-600 dark:text-gray-300" fill="currentColor" viewBox="0 0 20 20">
              <path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"></path>
            </svg>
            <svg id="theme-toggle-light-icon" class="hidden w-5 h-5 text-gray-600 dark:text-gray-300" fill="currentColor" viewBox="0 0 20 20">
              <path d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1zM5.05 6.464A1 1 0 106.465 5.05l-.708-.707a1 1 0 00-1.414 1.414l.707.707zm1.414 8.486l-.707.707a1 1 0 01-1.414-1.414l.707-.707a1 1 0 011.414 1.414zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" fill-rule="evenodd" clip-rule="evenodd"></path>
            </svg>
          </button>
        </div>
        <button onclick="openSettingsModal()" class="mt-2 w-full bg-blue-500 hover:bg-blue-600 dark:bg-blue-600 dark:hover:bg-blue-700 text-white px-4 py-2 rounded text-sm font-medium transition">
          Add Feed
        </button>
        <button onclick="refreshAllFeeds()" class="mt-2 w-full bg-gray-100 hover:bg-gray-200 dark:bg-gray-700 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-200 px-4 py-2 rounded text-sm font-medium transition">
          Refresh All
        </button>
      </div>
      
      <!-- Feed list -->
      <nav class="flex-1 overflow-y-auto p-2">
        <!-- All feeds -->
        <a href="/" class="flex items-center px-3 py-2 rounded hover:bg-gray-100 dark:hover:bg-gray-700 transition <%= !currentFeed ? 'bg-blue-50 dark:bg-blue-900 text-blue-600 dark:text-blue-400' : 'text-gray-700 dark:text-gray-300' %>">
          <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
          </svg>
          <span class="flex-1 font-medium">All</span>
          <% if (totalUnread > 0) { %>
            <span id="totalUnreadBadge" class="bg-blue-500 dark:bg-blue-600 text-white text-xs px-2 py-1 rounded-full"><%= totalUnread %></span>
          <% } %>
        </a>
        
        <!-- Individual feeds -->
        <div class="mt-2 space-y-1">
          <% feeds.forEach(feed => { %>
            <div class="feed-item-container relative" data-feed-id="<%= feed.id %>">
              <a href="/?feed=<%= feed.id %>" class="flex items-center px-3 py-2 rounded hover:bg-gray-100 dark:hover:bg-gray-700 transition <%= currentFeed && currentFeed.id === feed.id ? 'bg-blue-50 dark:bg-blue-900 text-blue-600 dark:text-blue-400' : 'text-gray-700 dark:text-gray-300' %>">
                <!-- Icon container that swaps with settings icon on hover -->
                <div class="w-5 h-5 mr-3 flex items-center justify-center relative">
                  <% if (feed.icon_url) { %>
                    <img src="<%= feed.icon_url %>" alt="" class="feed-icon w-5 h-5 rounded" onerror="this.style.display='none'">
                  <% } else { %>
                    <div class="feed-icon w-5 h-5 rounded" style="background-color: <%= feed.color %>"></div>
                  <% } %>
                  <!-- Settings icon appears on hover -->
                  <svg onclick="event.preventDefault(); event.stopPropagation(); editFeed(<%= feed.id %>, '<%= feed.title.replace(/'/g, "\\'") %>', '<%= feed.url.replace(/'/g, "\\'") %>', '<%= feed.icon_url ? feed.icon_url.replace(/'/g, "\\'") : '' %>', '<%= feed.color %>')" 
                       class="settings-icon w-5 h-5 text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-300 cursor-pointer absolute inset-0" 
                       fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                  </svg>
                </div>
                <span class="flex-1 truncate text-sm"><%= feed.title %></span>
                <% if (unreadCounts[feed.id] > 0) { %>
                  <span class="feed-unread-badge text-xs px-2 py-1 rounded-full" style="background-color: <%= feed.color %>; color: white;"><%= unreadCounts[feed.id] %></span>
                <% } %>
              </a>
            </div>
          <% }) %>
        </div>
      </nav>
    </aside>
    
    <!-- Overlay for mobile -->
    <div id="sidebarOverlay" class="hidden md:hidden fixed inset-0 bg-black bg-opacity-50 z-30" onclick="toggleSidebar()"></div>
    
    <!-- Main content -->
    <main class="flex-1 overflow-y-auto w-full md:w-auto">
      <div class="max-w-4xl mx-auto p-6 pt-20 md:pt-6 pb-8">
        <!-- Header -->
        <div class="mb-6">
          <h2 class="text-2xl font-bold text-gray-800 dark:text-white">
            <%= currentFeed ? currentFeed.title : 'All Feeds' %>
          </h2>
          <p class="text-gray-500 dark:text-gray-400 text-sm mt-1">
            <%= items.length %> items
          </p>
        </div>
        
        <!-- Items -->
        <% if (items.length === 0) { %>
          <div class="text-center py-12">
            <svg class="w-16 h-16 mx-auto text-gray-300 dark:text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 5c7.18 0 13 5.82 13 13M6 11a7 7 0 017 7m-6 0a1 1 0 11-2 0 1 1 0 012 0z"></path>
            </svg>
            <p class="mt-4 text-gray-500 dark:text-gray-400">No items yet. Add a feed to get started!</p>
          </div>
        <% } else { %>
          <div class="space-y-4">
            <% items.forEach(item => { %>
              <article class="bg-white dark:bg-gray-800 rounded-lg shadow hover:shadow-md dark:hover:shadow-xl transition overflow-hidden feed-border-<%= item.read_at ? 'read' : 'unread' %>" 
                       style="<%= item.read_at ? '' : 'border-left-color: ' + item.feed_color + ';' %>"
                       data-item-id="<%= item.id %>"
                       data-feed-id="<%= item.feed_id %>"
                       data-feed-color="<%= item.feed_color %>">
                <div class="p-5">
                  <!-- Header with feed info -->
                  <div class="flex items-center mb-3">
                    <% if (item.feed_icon) { %>
                      <img src="<%= item.feed_icon %>" alt="" class="w-4 h-4 mr-2 rounded" onerror="this.style.display='none'">
                    <% } else { %>
                      <div class="w-4 h-4 mr-2 rounded" style="background-color: <%= item.feed_color %>"></div>
                    <% } %>
                    <span class="text-xs text-gray-500 dark:text-gray-400 font-medium"><%= item.feed_title %></span>
                    <span class="mx-2 text-gray-300 dark:text-gray-600">•</span>
                    <time class="text-xs text-gray-500 dark:text-gray-400"><%= new Date(item.pub_date).toLocaleDateString('de-DE') %></time>
                    <% if (!item.read_at) { %>
                      <span class="ml-2 w-2 h-2 rounded-full" style="background-color: <%= item.feed_color %>"></span>
                    <% } %>
                  </div>
                  
                  <!-- Content -->
                  <a href="<%= item.link %>" target="_blank" rel="noopener noreferrer" class="block" onclick="markAsRead(<%= item.id %>, event)">
                    <div class="flex gap-4">
                      <div class="flex-1">
                        <h3 class="text-lg font-semibold text-gray-800 dark:text-white hover:text-blue-600 dark:hover:text-blue-400 transition line-clamp-2 mb-2">
                          <%= item.title %>
                        </h3>
                        <p class="text-gray-600 dark:text-gray-300 text-sm line-clamp-3">
                          <%= item.description %>
                        </p>
                      </div>
                      
                      <% if (item.image_url) { %>
                        <div class="w-32 h-32 flex-shrink-0">
                          <img src="<%= item.image_url %>" alt="" class="w-full h-full object-cover rounded" onerror="this.parentElement.style.display='none'">
                        </div>
                      <% } %>
                    </div>
                  </a>
                  
                  <!-- Actions -->
                  <div class="mt-3 flex gap-2">
                    <button onclick="toggleRead(<%= item.id %>, <%= item.read_at ? 'false' : 'true' %>)" 
                            class="text-xs text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-200 px-2 py-1 rounded hover:bg-gray-100 dark:hover:bg-gray-700 transition">
                      <%= item.read_at ? 'Mark Unread' : 'Mark Read' %>
                    </button>
                  </div>
                </div>
              </article>
            <% }) %>
          </div>
        <% } %>
      </div>
    </main>
  </div>
  
  <!-- Settings Modal -->
  <div id="settingsModal" class="hidden fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
    <div class="bg-white dark:bg-gray-800 rounded-lg shadow-xl w-full max-w-md mx-4" onclick="event.stopPropagation()">
      <div class="p-6">
        <div class="flex items-center justify-between mb-4">
          <h3 class="text-xl font-bold text-gray-800 dark:text-white" id="modalTitle">Add Feed</h3>
          <button onclick="closeSettingsModal()" class="text-gray-400 hover:text-gray-600 dark:hover:text-gray-300">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
        
        <form id="feedForm" class="space-y-4">
          <input type="hidden" id="feedId" name="feedId">
          
          <div>
            <label for="feedTitle" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Title</label>
            <input type="text" id="feedTitle" name="title" required 
                   class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-white rounded focus:outline-none focus:ring-2 focus:ring-blue-500">
          </div>
          
          <div>
            <label for="feedUrl" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
              Feed URL
              <span class="text-xs text-gray-500 dark:text-gray-400">- Icon auto-detected</span>
            </label>
            <input type="url" id="feedUrl" name="url" required 
                   class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-white rounded focus:outline-none focus:ring-2 focus:ring-blue-500"
                   placeholder="https://example.com/feed.xml">
          </div>
          
          <div>
            <label for="feedIcon" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
              Icon URL (auto-filled)
            </label>
            <div class="flex gap-2">
              <input type="url" id="feedIcon" name="icon_url" 
                     class="flex-1 px-3 py-2 border border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-white rounded focus:outline-none focus:ring-2 focus:ring-blue-500"
                     placeholder="Auto-detected from feed URL">
              <button type="button" id="detectColorBtn" onclick="detectColorFromIcon()" 
                      class="px-3 py-2 bg-gray-100 hover:bg-gray-200 dark:bg-gray-700 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-200 rounded text-sm font-medium transition"
                      title="Detect color from icon">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01"></path>
                </svg>
              </button>
            </div>
          </div>
          
          <div>
            <label for="feedColor" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
              Color (auto-detected)
            </label>
            <input type="color" id="feedColor" name="color" value="#3b82f6"
                   class="w-full h-10 rounded cursor-pointer">
          </div>
          
          <div class="flex gap-2 pt-2">
            <button type="submit" class="flex-1 bg-blue-500 hover:bg-blue-600 dark:bg-blue-600 dark:hover:bg-blue-700 text-white px-4 py-2 rounded font-medium transition">
              Save
            </button>
            <button type="button" id="deleteFeedBtn" onclick="deleteFeed()" 
                    class="hidden bg-red-500 hover:bg-red-600 dark:bg-red-600 dark:hover:bg-red-700 text-white px-4 py-2 rounded font-medium transition">
              Delete
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
  
  <!-- Confirmation Modal -->
  <div id="confirmModal" class="hidden fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
    <div class="bg-white dark:bg-gray-800 rounded-lg shadow-xl w-full max-w-md mx-4" onclick="event.stopPropagation()">
      <div class="p-6">
        <h3 class="text-lg font-bold text-gray-800 dark:text-white mb-4" id="confirmTitle">Confirm Action</h3>
        <p class="text-gray-600 dark:text-gray-300 mb-6" id="confirmMessage">Are you sure?</p>
        <div class="flex gap-3 justify-end">
          <button onclick="closeConfirmModal()" class="px-4 py-2 text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 rounded transition">
            Cancel
          </button>
          <button id="confirmButton" class="px-4 py-2 bg-red-500 hover:bg-red-600 dark:bg-red-600 dark:hover:bg-red-700 text-white rounded transition">
            Confirm
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- Alert Modal -->
  <div id="alertModal" class="hidden fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
    <div class="bg-white dark:bg-gray-800 rounded-lg shadow-xl w-full max-w-md mx-4" onclick="event.stopPropagation()">
      <div class="p-6">
        <div class="flex items-start gap-3 mb-4">
          <div id="alertIcon" class="flex-shrink-0">
            <!-- Icon will be inserted here -->
          </div>
          <div class="flex-1">
            <h3 class="text-lg font-bold text-gray-800 dark:text-white mb-2" id="alertTitle">Notice</h3>
            <p class="text-gray-600 dark:text-gray-300" id="alertMessage">Message</p>
          </div>
        </div>
        <div class="flex justify-end">
          <button onclick="closeAlertModal()" class="px-4 py-2 bg-blue-500 hover:bg-blue-600 dark:bg-blue-600 dark:hover:bg-blue-700 text-white rounded transition">
            OK
          </button>
        </div>
      </div>
    </div>
  </div>

  <script src="/app.js"></script>
</body>
</html>
